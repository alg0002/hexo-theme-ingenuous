<% if (theme.about) { %>
  <div class="widget-wrap">
    <h3 id="widget-title-about" class="widget-title">About</h3>
    <div class="widget">
      <% if (theme.about.author) { %>
        <p> <%- config.author %> </p>
      <% } %>
      <% if (theme.about.description) { %>
        <p> <%- config.description %> </p>
      <% } %>
      <% if (theme.about.twitter) { %>
        <p> Twitter: <a href="https://twitter.com/<%- theme.twitter %>" target="_blank">@<%- theme.twitter %></a> </p>
      <% } %>
      <% if (theme.about.aboutme) { %>
        <p> <%- theme.aboutme %> </p>
      <% } %>
    </div>
  </div>
<% } %>
